[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm run build"

[deploy]
startCommand = "npm start"
healthcheckPath = "/api/knowledge-base/status"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
